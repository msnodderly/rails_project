<h1>Posts</h1>
<ul> 
  <% @posts.each do |post| %>
    <% domain = post.url.gsub("http://","") %>
    <li> <%= link_to post.title, post.url %> (<%= domain %>) <br>(<%= post.comments.count %> comments)</li>
  <% end %>
</ul>  
